apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helm-nameko-devex.fullname" . }}-test-connection"
  labels:
    {{- include "helm-nameko-devex.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helm-nameko-devex.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
