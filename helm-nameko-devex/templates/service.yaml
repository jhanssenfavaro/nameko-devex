apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-nameko-devex.fullname" . }}
  labels:
    {{- include "helm-nameko-devex.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm-nameko-devex.selectorLabels" . | nindent 4 }}
